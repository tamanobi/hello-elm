version: 2

defaults: &defaults
  working_directory: ~/repo
  docker:
    - image: circleci/node:11.10.1

jobs:
  test:
    <<: *defaults
    steps:
      - checkout

      - restore_cache:
          keys:
            - v1-dependencies-v2-{{ checksum "elm.json" }}
            # fallback to using the latest cache if no exact match is found
            - v1-dependencies-

      - run: rm -rf /home/circleci/.elm
      - run: npm install
      - run:
          name: Run tests
          command: npm test

      - save_cache:
          paths:
            - elm-stuff
            - node_modules
          key: v1-dependencies-v2-{{ checksum "elm.json" }}

      - persist_to_workspace:
          root: ~/repo
          paths: .

workflows:
  version: 2
  only-test:
    jobs:
      - test:
          filters:
            tags:
              only: /^v.*/
