version: 2

defaults: &defaults
  working_directory: ~/repo
  docker:
    - image: circleci/node:8.9.1

jobs:
  test:
    <<: *defaults
    steps:
      - checkout

      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "elm.json" }}
            # fallback to using the latest cache if no exact match is found
            - v1-dependencies-

      - run: npm install && npx elm install
      - run:
          name: Run tests
          command: npx elm test
      - run:
          name: Build
          command: npx elm make src/Main.elm

      - save_cache:
          paths:
            - elm-stuff
            - node_modules
          key: v1-dependencies-{{ checksum "elm.json" }}

      - persist_to_workspace:
          root: ~/repo
          paths: .

workflows:
  version: 2
  only-test:
    jobs:
      - test:
          filters:
            tags:
              only: /^v.*/
